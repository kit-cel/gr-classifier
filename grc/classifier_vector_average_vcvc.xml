<?xml version="1.0"?>
<block>
  <name>vector_average_vcvc</name>
  <key>Vector Average</key>
  <category>[CLASSIFIER]</category>
  <import>import classifier</import>
  <make>classifier.vector_average_vcvc($n_samples, $vlen)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Num Samples</name>
    <key>n_samples</key>
    <type>int</type>
  </param>

  <param>
    <name>Vec length</name>
    <key>vlen</key>
    <value>1</value>
    <type>int</type>
  </param>
  <check>$vlen >= 1</check>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
    <vlen>$vlen</vlen>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>float</type>
    <vlen>$vlen</vlen>
  </source>
</block>
